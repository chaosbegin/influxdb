// This file is intentionally left nearly empty.
// Protobuf compilation for replication services is now handled by the `influxdb3_proto` crate.
// If other server-specific protobufs are added in the future, their compilation
// can be configured here.
fn main() -> Result<(), Box<dyn std::error::Error>> {
    Ok(())
}
